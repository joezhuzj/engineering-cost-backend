# Render 部署指南

## 📦 部署步骤

### 方法1：从GitHub部署（推荐）

#### 1. 准备GitHub仓库
```bash
cd F:\CODE\Tes\backend

# 初始化git（如果还没有）
git init

# 添加文件
git add .

# 提交
git commit -m "Initial backend commit"

# 创建GitHub仓库并推送
# 访问 https://github.com/new 创建新仓库
# 然后执行：
git remote add origin https://github.com/你的用户名/仓库名.git
git branch -M main
git push -u origin main
```

#### 2. 在Render创建服务

1. **访问** https://render.com
2. **注册/登录**（推荐用GitHub登录）
3. **点击** "New +" → "Web Service"
4. **连接GitHub仓库**
   - 选择你刚创建的backend仓库
   - 点击 "Connect"

5. **配置服务**：
   - **Name**: `engineering-cost-api`（或自定义）
   - **Region**: `Singapore` 或 `Oregon`（选离中国近的）
   - **Branch**: `main`
   - **Root Directory**: 留空（或填 `backend` 如果代码在子目录）
   - **Environment**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Plan**: 选择 **Free** ✅

6. **高级设置**（点击 "Advanced"）
   - 添加环境变量：
     - `NODE_ENV` = `production`
     - `JWT_SECRET` = `随机字符串`（如：`abc123xyz789`）

7. **点击** "Create Web Service"

8. **等待部署**（3-5分钟）
   - 部署完成后会得到URL：`https://engineering-cost-api.onrender.com`

---

### 方法2：手动上传（不推荐但可用）

如果不想用GitHub：

1. 将 `backend` 文件夹打包成 `.zip`
2. 访问 Render控制台
3. 选择 "Manual Deploy"
4. 上传zip文件

---

## ⚙️ 环境变量配置

在Render服务设置中添加：

| 变量名 | 值 | 说明 |
|--------|---|------|
| `NODE_ENV` | `production` | 环境 |
| `PORT` | `3000` | 端口（Render会自动覆盖）|
| `JWT_SECRET` | 随机字符串 | JWT密钥 |

---

## 🔧 数据库说明

**重要**：Render的文件系统是临时的！

### 问题
- SQLite数据会在重启后丢失
- 每次部署都会清空数据

### 解决方案

**选项A：使用Render PostgreSQL（推荐）**
1. 创建免费PostgreSQL数据库
2. 修改代码使用PostgreSQL而不是SQLite
3. 只需要改 `config/database.js`

**选项B：使用外部数据库**
- Railway PostgreSQL（免费）
- Supabase（免费PostgreSQL）
- MongoDB Atlas（免费）

**选项C：接受临时数据**
- 适合测试
- 每次重启会重置数据

---

## 🌐 部署后

### 1. 获取API地址
部署完成后会得到：
```
https://你的服务名.onrender.com
```

### 2. 测试API
```bash
# 测试根路径
curl https://你的服务名.onrender.com

# 测试登录
curl -X POST https://你的服务名.onrender.com/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

### 3. 连接前端
修改前端代码，将API地址改为：
```javascript
const API_BASE_URL = 'https://你的服务名.onrender.com';
```

---

## ⚠️ 注意事项

### 1. 服务会休眠
- 15分钟无请求会自动休眠
- 下次访问需要等30秒唤醒
- 唤醒后正常运行

### 2. 数据持久化问题
- SQLite数据不持久化
- 建议改用PostgreSQL

### 3. 免费额度
- ✅ 750小时/月
- ✅ 100GB带宽/月
- ✅ 完全够用

---

## 🔄 更新部署

### 自动部署
如果连接了GitHub：
```bash
git add .
git commit -m "更新"
git push
```
Render会自动检测并重新部署！

### 手动部署
在Render控制台点击 "Manual Deploy" → "Deploy latest commit"

---

## 🆘 常见问题

### 1. 部署失败
- 检查 `package.json` 中的 `start` 脚本
- 确保 `npm install` 能成功运行

### 2. 无法访问
- 等待部署完成（看日志）
- 检查环境变量是否设置

### 3. 数据丢失
- 这是正常的（SQLite不持久化）
- 改用PostgreSQL解决

---

## 📊 监控

在Render控制台可以：
- 查看实时日志
- 监控请求数量
- 查看资源使用

---

## 💡 优化建议

### 1. 防止休眠
使用定时ping服务（如cron-job.org）每10分钟访问一次

### 2. 数据库升级
切换到PostgreSQL：
```bash
# 1. 在Render创建PostgreSQL数据库
# 2. 修改 config/database.js
# 3. 安装pg: npm install pg
```

### 3. 域名绑定
在Render设置中可以绑定自定义域名（免费）
